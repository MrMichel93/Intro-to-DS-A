# Linked List Operations Visualizations

Visual representations of linked list operations and comparisons with arrays.

## Singly Linked List Structure

```
Node Structure:
┌──────────┬──────────┐
│   Data   │   Next   │
└──────────┴──────────┘
     │          │
     │          └─── Pointer to next node
     └── Stored value

Complete Linked List:
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘

Each arrow (•) represents a pointer/reference to the next node
null indicates the end of the list
```

## Insert at Beginning - O(1)

```
Insert 5 at the beginning:

Step 1: Create new node
┌────┬────┐
│  5 │null│  ← New node
└────┴────┘

Step 2: Point new node to current head
    ┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
    │  5 │ •─┼───→│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │null│
    └────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
      ↑
   New node

Step 3: Update head to point to new node
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│  5 │ •─┼───→│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘

Total: 3 operations (constant time!)
No shifting required unlike arrays!
```

## Insert at End - O(n)

```
Insert 40 at the end (without tail pointer):

    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴────┘
  ↓
current

Step 1: Traverse to end
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴────┘
                                  ↑
                              current (found end)

Step 2: Create and attach new node
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘

Must traverse n nodes: O(n)

With tail pointer: O(1)
    head                            tail
     ↓                               ↓
┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴────┘
```

## Insert in Middle - O(n)

```
Insert 25 after node with value 20:

Step 1: Find the position (traverse until value 20)
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴────┘
                  ↑
              current (found it!)

Step 2: Create new node
┌────┬────┐
│ 25 │null│  ← New node
└────┴────┘

Step 3: Update pointers
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 25 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
                  │                            ↑
                  └────────────────────────────┘
                  (old connection)

Wait, that's wrong! Correct order:

Step 3a: Point new node to next node
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ • │    │ 25 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
                              ↑
                         new_node.next = current.next

Step 3b: Point current node to new node
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 25 │ •─┼───→│ 30 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
                  
                current.next = new_node

Only 2 pointer changes, but finding position: O(n)
```

## Delete a Node - O(n)

```
Delete node with value 20:

Initial:
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘

Step 1: Find node to delete and its previous node
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
  ↑             ↑
 prev        current (to delete)

Step 2: Update previous node's pointer
    head
     ↓
┌────┬───┐              ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼─────────────→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘              └────┴───┘    └────┴────┘
  ↑                       
 prev.next = current.next

Node with value 20 is now unreachable (garbage collected)

Just update one pointer, but finding node: O(n)
```

## Delete Head Node - O(1)

```
Delete the first node:

Before:
    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘

After (just move head pointer):
              head
               ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼─  →│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
(unreachable)

head = head.next

Constant time! No traversal needed.
```

## Traversal - O(n)

```
Print all values:

    head
     ↓
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
  ↓
current

Step 1: Print 10, move to next
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
                  ↓
              current

Step 2: Print 20, move to next
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
                                  ↓
                              current

Step 3: Print 30, move to next
┌────┬───┐    ┌────┬───┐    ┌────┬───┐    ┌────┬────┐
│ 10 │ •─┼───→│ 20 │ •─┼───→│ 30 │ •─┼───→│ 40 │null│
└────┴───┘    └────┴───┘    └────┴───┘    └────┴────┘
                                                  ↓
                                              current

Step 4: Print 40, move to next (null - stop)

Must visit every node: O(n)
```

## Doubly Linked List

```
Each node has two pointers:

Node Structure:
┌──────┬──────┬──────┐
│ Prev │ Data │ Next │
└──────┴──────┴──────┘

Complete Doubly Linked List:
         head                                        tail
          ↓                                           ↓
┌────┬────┬───┐    ┌───┬────┬───┐    ┌───┬────┬───┐    ┌───┬────┬────┐
│null│ 10 │ •─┼───→│ ← │ 20 │ •─┼───→│ ← │ 30 │ •─┼───→│ ← │ 40 │null│
└────┴────┴───┘    └───┴────┴───┘    └───┴────┴───┘    └───┴────┴────┘
                      ↑__________________|                ↑___________|

Can traverse in both directions!
```

## Array vs Linked List Comparison

```
Scenario: Insert element at beginning

ARRAY (size 4):
Before: [10][20][30][40]

Step 1-4: Shift each element right
        [  ][10][20][30][40]  (need extra space!)
        
Step 5: Insert
        [5 ][10][20][30][40]

Operations: n shifts + 1 insert = O(n)


LINKED LIST:
Before: [10]→[20]→[30]→[40]→null

Step 1: Create node [5]
Step 2: Point [5]→[10]
Step 3: Update head to [5]

After:  [5]→[10]→[20]→[30]→[40]→null

Operations: 3 pointer changes = O(1)

Winner: Linked List! 🏆
```

```
Scenario: Access 3rd element

ARRAY:
[10][20][30][40]
  0   1   2   3
         ↑
Direct access: array[2]
Operations: 1 = O(1)


LINKED LIST:
[10]→[20]→[30]→[40]→null
 ↓    ↓    ↓
Step1 Step2 Step3

Must traverse from head
Operations: 3 steps = O(n)

Winner: Array! 🏆
```

## Memory Layout Comparison

```
ARRAY in Memory (contiguous):
┌────┬────┬────┬────┬────┐
│ 10 │ 20 │ 30 │ 40 │ 50 │
└────┴────┴────┴────┴────┘
1000 1004 1008 1012 1016

✅ Cache-friendly (nearby elements loaded together)
✅ No extra space for pointers
❌ Fixed size or expensive resize


LINKED LIST in Memory (scattered):
    ┌────┬────┐         ┌────┬────┐
    │ 10 │1084│         │ 20 │1204│
    └────┴────┘         └────┴────┘
    @1000               @1084
                              
    ┌────┬────┐         ┌────┬────┐
    │ 30 │2000│         │ 40 │null│
    └────┴────┘         └────┴────┘
    @1204               @2000

❌ Not cache-friendly (nodes anywhere in memory)
❌ Extra space for pointers (8 bytes per node on 64-bit)
✅ Dynamic size, easy to grow
```

## When to Use Each

```
Use ARRAY when:
✓ Need fast random access
✓ Know size ahead of time
✓ Reading more than writing
✓ Want cache performance

Use LINKED LIST when:
✓ Frequent insertions at beginning
✓ Don't know final size
✓ Need to insert/delete in middle often
✓ Don't need random access
```

## Key Takeaways

**Linked Lists**:
- ✅ Insert at head: O(1)
- ⚠️ Insert at tail (no tail pointer): O(n)
- ✅ Insert at tail (with tail pointer): O(1)
- ⚠️ Access by index: O(n)
- ⚠️ Search: O(n)
- 📦 Extra memory for pointers

**Trade-off**: Flexibility vs Speed
